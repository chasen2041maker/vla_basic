# Learning Method｜围绕项目边做边教

角色、接棒顺序、双轨进度的唯一规范是 [AGENTS.md](AGENTS.md)。当前项目是 HighwayEnv 驾驶实验；旧 Lab 是按需材料，不是前置通关路线。

## 1. 默认学习循环

**一个驾驶问题 → GPT 先讲并示范分析 → 学习者反馈 → 必要补讲 → 你与 Codex 有限实践 → GPT 核对 → 记录真实进度。**

项目持续改进，但不把做功能替代成教学，也不把所有基础讲完才允许运行。系统定位只讲清当前相关的一小段：输入是什么、谁负责、动作如何影响环境、如何看出失败。

## 2. 参考实现驱动，不是从空白抄工程

提供完整可运行的当前参考，让学习者能看到现象。GPT 带读相关调用链，解释框架隐藏的职责；暂时略过与本课无关的 CLI、存盘和样板细节。

一个主题可以分多次对话。第一次观察课可以只运行、预测和解释；到策略、坐标、评测等关键行为时，再保留一个需要学习者理解并控制的小变化。不要提前为每个概念创建 Demo、工厂或空课程目录。

## 3. 已有能力验证，新领域认真讲

Python、Agent/workflow、常规训练和学过的蒸馏量化可作为起点，不机械重讲；自述不是通过证据。视觉、相机几何、车辆运动、动作、开放环/闭环和驾驶 VLA 不能因为会训练就跳过。

卡住时先判断是领域概念、输入语义、执行链还是代码语法，再缩小问题和补讲。不能只重复定义或继续增加任务。

## 4. 一次实验怎样才有学习价值

先作预测，固定必要条件，只改变本次要研究的变量；随后对照真实日志和测试。区分：模拟器状态与视觉输入、目标速度与实际速度、策略频率与物理更新、环境结束与运行器停止。

老师可以先演示一个例子，但必须标明示范来源。教材中的假设数字必须写“假设”；真实实验必须带版本、参数、执行者和证据位置。不能把老师/Codex 的答案或日志填成学习者独立完成。

## 5. 验证与证据

字段/单位/时间/坐标正确属于契约证据；输入消融、训练排错和动作解码属于模型证据；开放环/闭环和失败分类属于评测证据；延迟、过期检测、日志和 fallback 属于系统证据。

它们证明的范围不同。`tests green` 不等于驾驶安全，工程完成不等于个人掌握。具体任务按当前验收确认；不要给观察课强加训练要求，也不要让后期模型任务只靠复述通过。

## 6. 不提前堆前沿名词

强化学习前先能解释 state、action、transition、rollout、reward 与驾驶指标的区别。世界模型、VLA、蒸馏部署等保留在长期路线，需要时再取用；不替当前课堂新增第二条主线。

当前示范课程见 [H001 带读](experiments/highway_driving/walkthrough/H001-idle-step.md)，当前真正学到哪里只看 [PROGRESS.md](PROGRESS.md)。
