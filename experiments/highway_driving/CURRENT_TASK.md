# H001｜运行并解释一个驾驶回合

状态：**ACTIVE；代码基础设施已加入，学习者验证未完成。**

## 本次问题

**我给出 IDLE 后，为什么车还在动？一次 step 究竟过去多少时间？**

先解释：HighwayEnv 提供道路、模拟状态和底层控制；我们的脚本提供高层动作与记录。当前不是感知模型、跟车策略或 VLA。

## 本轮只做这些

1. 用固定种子运行 `run_episode.py --max-steps 1`，看到初始观察、下一观察和画面。
2. 结合 `make_env()` 与一条 `trace.jsonl`，说明字段、单位、相对/绝对量和两个时间。
3. **先预测**把动作从 IDLE 改成 SLOWER 会怎样，再用同种子运行核对。
4. 用自己的话说明结束原因，指出“进程成功”和“驾驶安全”的区别。

默认单步不会说明长期表现。后续再跑完整回合观察失败；不在这轮同时实现变道策略、奖励设计和训练。

## 阅读顺序

`README.zh-CN.md` 的观察/动作/时间契约 → `run_episode.py: make_env` → `reset/step` 循环 → 对应真实测试。先说问题和数据流，再拆局部代码，不机械从 Python 重讲。

## 验收不是抄答案

- [ ] 学习者自己运行并保留 Python/依赖版本、种子、动作、输出路径。
- [ ] 解释为什么自车第 0 行不是全零，为什么其他车辆的 vx 可能为负。
- [ ] 解释 `presence=0` 与 `order=sorted` 的边界。
- [ ] 解释 IDLE 为什么不停车，SLOWER 为什么不等于紧急制动。
- [ ] 从日志说出一次动作前后的时间，而不是猜“循环一次就是一秒”。
- [ ] 先预测再比较 IDLE/SLOWER，能指向具体日志字段。
- [ ] 区分碰撞、环境时限、运行器步数上限；不把 CI 通过当成个人掌握。

只有出现实际解释和实验记录后，才更新 `PROGRESS.md` 的学习状态。

## 旧 Lab 怎么用

搞不清“哪一层负责”，按需回看 Lab 000；搞不清字段/时间语义，按需回看 Lab 001。**不用先通关旧 Lab 才能做 H001。** 旧任务的未完成状态不变。

## 后续候选（尚未激活）

H002 再固定一个慢前车场景，定位前车与速度差，加入第一条跟车规则。真实出现独立策略职责时再创建 `policy.py`。当前不提前生成它。
