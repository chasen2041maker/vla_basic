# 2026-09-22｜HighwayEnv 项目入口调整

## 决策

以 `vla_basic` 承载持续改进的驾驶实验；保留旧 Lab 和长期 VLA 路线，不复制上游仓库，不另建项目，不提前引入训练框架。当前只做 H001：观察、明确动作、真实环境一步/单回合、可视化、记录和结束原因。

基础提交：`6b8a260183db5c168645e5b3ba1f58d7cead3669`。

## 工程与学习分开

工程实现已加入：中文入口、单回合脚本、参数检查、单位/坐标/时间契约、固定直接依赖、真实集成测试、Linux/Windows CI、日志/PNG/GIF 与版本摘要。

学习者尚未提交自己的运行结果、字段解释、动作预测或失败定位，不因此标记 PASSED。旧 Lab 000/001 的个人验收状态保持未完成。

## 验证记录

本地执行环境 Python 3.13.5：源码编译通过，3 个纯逻辑测试通过。直接 Git 克隆与 pip 安装因 DNS 解析失败，**没有在此本地环境运行 HighwayEnv，也没有声称在学习者电脑运行过**。

通过 GitHub 接口提交临时重构分支后，实际 CI 在 Python 3.12 上完成：

- [首轮 HighwayEnv CI](https://github.com/chasen2041maker/vla_basic/actions/runs/35694565190)：Ubuntu / Windows 均通过安装、pip check、旧 Lab 检查、10 个新测试与回合输出。
- [首轮旧 CI](https://github.com/chasen2041maker/vla_basic/actions/runs/35694565219)：通过。
- 对应首轮代码：`af1752cf187ae8a7917f55fd2046370219ba1676`。

首轮新测试为 3 个纯逻辑测试 + 7 个真实环境集成测试。旧 Lab 000 有 4 个单元测试，Lab 001 有 7 个单元测试。教学输出保持 `SYSTEM MAP RESULT: 4 / 4 PASS` 与 `BASELINE RESULT: 4 / 6 PASS`，后者不是两个单元测试失败。

首轮 smoke 命令为 `--max-steps 5 --output-dir outputs/ci-smoke`，实际记录 `runner_step_limit`、5 个决策步、1.000 秒模拟时间、IDLE。这是短回合工程检查，不是安全驾驶结论。

## 复核中发现并修正

首轮虽然通过，但安装日志出现 `pygame` 与上游所需 `pygame-ce` 同时安装、共享导入名的问题。候选最终版本移除 `pygame`，只固定 `pygame-ce==2.5.8`，摘要按真实分发包名称记版本。同时加入 `runner_sha256`，区别 HEAD 相同但脚本本地已改动的情况。

**以上首轮结果不能替代修正后的重新验证。最终验证记录应在后续检查完成后补充，不提前声称修正版已通过。**

## 限制

默认使用模拟器状态与恒定高层动作，没有实现感知、跟车避碰、轨迹学习、强化学习或 VLA。当前种子场景不是专门构造的慢前车案例。GIF 仅采样决策时刻，不包含全部物理帧。当前只锁直接依赖；完整解析版本由 CI artifact 的 pip-freeze 留档。

原生桌面窗口、学习者本机和 macOS 没有本次运行证据。CI 的图片/日志工件保留 14 天，过期后应重跑工作流，不假装链接永远可下载。
