# Learner Profile & Teaching Contract

这份文件保存会长期影响教学方式的公开技术上下文。

## 1. 当前技术背景

可以长期假设：

- 已有 Agent 工程工作经验；
- Python、RAG、Agent/workflow、tool calling 和 AI Coding 相对熟悉；
- 能阅读工程代码；
- 自述能够进行常规深度学习模型训练；
- 自述学过模型蒸馏与量化；
- 尚未学习强化学习；
- 不应默认具备计算机视觉、相机几何、自动驾驶数据、车辆运动、轨迹规划、控制或闭环评测背景。

自述能力用于决定“是否从基础重讲”，不自动进入 `PASSED`。后续会通过驾驶任务中的代码、实验和解释验证。

---

## 2. 长期目标

北极星是进入类似小鹏智能驾驶研发的技术链，而不是只会调用 VLA API。

目标控制力：

```text
看到一条驾驶样本
→ 说清字段、单位、时间和坐标
→ 画出传感器到车辆运动的完整链
→ 训练并修改轨迹模型
→ 分离 data / model / action / control / eval / system 故障
→ 比较开放环与闭环表现
→ 验证蒸馏、量化和部署影响
→ 约束输出并定义 fallback
→ 用实验而不是宣传语证明能力
```

---

## 3. 目标岗位层次

长期北极星：

- Driving VLM / VLA 算法研发；
- 端到端感知规划；
- 驾驶基础模型、世界模型与闭环学习；
- 动作表示、评测和车端模型优化。

现实桥梁方向：

- 智驾数据价值 / 数据闭环；
- 自动驾驶评测与仿真；
- AI Infra / 训练与推理平台；
- 端到端模型工程与部署。

仓库不会把桥梁方向写成降低目标，而是把它作为更适合当前背景的进入路径。

---

## 4. 教学顺序：系统优先

新主题第一次出现时，按下面顺序讲：

```text
它解决什么真实驾驶问题
→ 位于整车系统哪个位置
→ 上游输入是什么
→ 下游输出是什么
→ 属于什么时间尺度
→ 位于什么坐标系
→ 它负责什么
→ 它不负责什么
→ 正常例子
→ 失败症状
→ 如何验证
→ 最后再拆代码和公式
```

不允许一上来只按文件和类逐行讲，而没有说明其系统意义。

自动驾驶领域强制追问：

```text
这个值是什么时间的？
这个值在哪个坐标系？
这个 action 是轨迹、token 还是单步控制？
这个指标是开放环还是闭环？
```

---

## 5. 两条学习通道

### Verify 通道

适用于深度学习训练、Transformer、蒸馏、量化、Linux/Git 等已有经验：

```text
不重复基础课
→ 给真实驾驶任务
→ 独立修改或排错
→ 用测试和实验确认
```

### Learn 通道

适用于视觉、几何、车辆运动、闭环评测和 VLA：

```text
系统全景
→ mental model
→ 可运行 reference
→ 小改
→ fault injection
→ tests/eval
→ 独立解释
```

---

## 6. Reference Implementation 要求

参考实现应：

- 规模适中且能直接运行；
- 输入输出契约显式；
- 时间戳、单位和坐标写进变量名；
- 含预期输出、tests 和 failure cases；
- 明确哪些是教学简化；
- 不用高级框架隐藏当前知识；
- 不要求从空白重复写无价值样板。

学习者至少完成：

```text
读懂 execution path
+ 独立修改一个关键行为
+ 写或改一个测试
+ 定位一个故障
+ 用自己的话解释边界
```

---

## 7. AI Coding 使用边界

允许使用 ChatGPT、Codex 和其他 Coding Agent，但必须保留以下能力：

- 不依赖 Agent 也能读懂核心执行链；
- 能用日志、断点、最小复现定位关键 bug；
- 能判断 Agent 生成的时间、坐标、action 和 evaluator 逻辑是否正确；
- 能解释最终代码为什么成立以及何时失效。

“Agent 写完、测试全绿”不能单独构成掌握证据。

---

## 8. 掌握状态

```text
ASSUMED  根据经验或自述暂时作为教学起点
LEARNING 当前系统学习
REVIEW   基本理解或学过，但仓库证据不足
PASSED   有代码、测试、实验和解释证据
REVISIT  曾经通过，但需要重新验证
```

---

## 9. 不应该做的事情

不要：

- 从 Python 或普通神经网络基础重新开始；
- 因为会训练模型就跳过视觉、几何和车辆运动；
- 先背 PPO、DPO、GRPO，再理解驾驶 state/action/rollout；
- 把机械臂 VLA 当驾驶 VLA；
- 一上来下载重型数据和框架；
- 用轨迹 L2 单指标代替驾驶能力；
- 把自然语言推理当作安全证明；
- 把量产闭源宣传写成可复现结果；
- 为了显得先进无条件加入 world model、RL、multi-agent 或 Kubernetes。
